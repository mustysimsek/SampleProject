@model Support.WebApi.Models.GetProductViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section ProductDetails{

    <div class="row border-bottom" style="padding-top: 8px">
        <div class="col-md-3">
            <div class="input mb-3">
                <input type="text" class="form-control" placeholder="Ürün Kodu" id="txtProductCode" name="txtProductCode">
            </div>
        </div>
        <div class="col-md-2">
            <input class="btn btn-outline-primary" type="button" value="Getir" onclick="location.href='@Url.Action("GetProductByCode", "Product")?id=' + document.getElementById('txtProductCode').value" />
            @*            <input class="btn btn-outline-primary" type="submit" id="button-listele"
        onclick="location.href='@(Url.Action("GetProductByCode","Product")?id=' + document.getElementById('Id_Of_Textbox').value"/>
        Getir
        </button>*@
        </div>
    </div>
}
    <div class="table-responsive">
        <table class="table table-striped table-sm">
            <thead>
                <tr>
                    <th scope="col">
                        @Html.DisplayNameFor(model => model.Id)
                    </th>
                    <th scope="col">
                        @Html.DisplayNameFor(model => model.CategoryId)
                    </th>
                    <th scope="col">
                        @Html.DisplayNameFor(model => model.CreatedOn)
                    </th>
                    <th scope="col">
                        @Html.DisplayNameFor(model => model.Code)
                    </th>
                    <th scope="col">
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => modelItem.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => modelItem.CategoryId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => modelItem.CreatedOn)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => modelItem.Code)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => modelItem.Name)
                    </td>
                </tr>
            </tbody>
        </table>
    </div>

    <script>
        const input = document.querySelector('input');
        const log = document.getElementById('log');

        input.addEventListener('change', updateValue);

        function updateValue(e) {
            log.te = e.target.value;
        }
    </script>